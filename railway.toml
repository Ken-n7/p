[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'python manage.py migrate --noinput && python manage.py createsuperuser --noinput || true && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2'"
restartPolicyType = "on_failure"
